# BTC Sentiment-Driven LSTM Trading Pipeline — Python dependencies
#
# PINNED for ML reproducibility. Every dependency is pinned to an exact
# version so that `pip install -r requirements.txt` produces the same
# environment every time. Bump versions deliberately via a PR, not by
# accident via `>=`.
#
# Tested with Python 3.11 / 3.12 on Linux (CI), macOS, and Colab T4 GPU.

# ---- Data ingestion ----
yfinance==0.2.40
pandas==3.0.3
numpy==2.5.0
requests==2.32.3

# ---- NLP / Sentiment ----
transformers==4.44.2
torch==2.4.1
tqdm==4.66.5

# ---- ML / Deep Learning ----
# tensorflow-cpu is used instead of tensorflow to avoid GPU driver issues
# on Streamlit Cloud and CI. For local GPU training, install
# `tensorflow==2.17.0` instead.
tensorflow-cpu==2.17.0
scikit-learn==1.9.0

# ---- Hyperparameter optimization (Phase 2) ----
optuna==3.6.1

# ---- Model interpretability (Phase 2) ----
shap==0.46.0

# ---- Parquet caching for FinBERT scores (Phase 2) ----
pyarrow==17.0.0

# ---- Visualization ----
matplotlib==3.9.2
seaborn==0.13.2

# ---- Interactive dashboard (Streamlit app) ----
streamlit==1.39.0
plotly==5.24.1

# ---- Notebook runner ----
jupyter==1.1.1
ipykernel==7.3.0
nbconvert==7.16.4
ipykernel==6.29.5
nbconvert==7.17.1

# ---- Dev / test / lint (not needed at runtime) ----
# These are installed by `make setup` but are not imported by the app.
# Listed here so pip-audit covers them too.
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.15.20
pip-audit==2.7.3
bandit==1.7.10
